// When using Docker, this file is generated from LIB_LANG and LIB_NAME env vars.
// Edit here only when opening index.html directly in a browser (no Docker).
window.LIB_CONFIG = {
  lang: 'en',           // 'en' or 'de'
  name: 'the Library',  // shown in the header

  // Android app: set this to an HTTPS endpoint that serves the current books.json.
  // The native app caches the last successfully downloaded copy for offline use.
  // Leave null to keep using the bundled/local books.json.
  booksUrl: null,
};
